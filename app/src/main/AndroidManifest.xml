<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pendaftaran.ktp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PendaftaranKTP">
        <activity android:name=".admin.HomepageDetail"></activity>
        <activity android:name=".auth.RegistrationDetailActivity" />
        <activity android:name=".auth.RegistrationActivity" />
        <activity android:name=".utils.RegisterActivity" />
        <activity android:name=".admin.HomepageActivity" />
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>